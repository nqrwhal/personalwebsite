import MonitorDesktop from '../../components/MonitorDesktop';

/** Workstation reached from the responsive monitor transition. */
export default function ResponsiveDesktopReview() {
  return <MonitorDesktop mobileLayout responsiveLayout />;
}
